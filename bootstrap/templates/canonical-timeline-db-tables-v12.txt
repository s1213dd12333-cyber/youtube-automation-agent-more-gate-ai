      // Phase 11.12.2 — Canonical Timeline state
      `CREATE TABLE IF NOT EXISTS serialized_timeline_states (
        series_id TEXT PRIMARY KEY,
        revision_number INTEGER NOT NULL DEFAULT 0,
        last_chronology_index INTEGER NOT NULL DEFAULT 0,
        created_at TEXT DEFAULT CURRENT_TIMESTAMP,
        updated_at TEXT DEFAULT CURRENT_TIMESTAMP,
        FOREIGN KEY (series_id) REFERENCES serialized_series_bibles(id)
      )`,
      `CREATE TABLE IF NOT EXISTS serialized_timeline_events (
        id TEXT PRIMARY KEY,
        series_id TEXT NOT NULL,
        event_key TEXT NOT NULL,
        chronology_index INTEGER NOT NULL,
        episode_number INTEGER NOT NULL DEFAULT 0,
        scene_id TEXT,
        scene_order INTEGER,
        story_time_label TEXT,
        event_type TEXT NOT NULL DEFAULT 'story_event',
        summary TEXT NOT NULL,
        participants TEXT NOT NULL DEFAULT '[]',
        location_refs TEXT NOT NULL DEFAULT '[]',
        cause_event_ids TEXT NOT NULL DEFAULT '[]',
        consequences TEXT NOT NULL DEFAULT '[]',
        must_follow_event_ids TEXT NOT NULL DEFAULT '[]',
        must_precede_event_ids TEXT NOT NULL DEFAULT '[]',
        truth_status TEXT NOT NULL DEFAULT 'confirmed',
        revision_number INTEGER NOT NULL DEFAULT 1,
        status TEXT NOT NULL DEFAULT 'active',
        created_by TEXT,
        commit_id TEXT,
        created_at TEXT DEFAULT CURRENT_TIMESTAMP,
        updated_at TEXT DEFAULT CURRENT_TIMESTAMP,
        UNIQUE(series_id, event_key),
        UNIQUE(series_id, chronology_index),
        FOREIGN KEY (series_id) REFERENCES serialized_series_bibles(id)
      )`,
      `CREATE INDEX IF NOT EXISTS idx_serialized_timeline_series_chronology
        ON serialized_timeline_events(series_id, chronology_index)`,
      `CREATE INDEX IF NOT EXISTS idx_serialized_timeline_series_episode
        ON serialized_timeline_events(series_id, episode_number, scene_order)`,
      `CREATE TABLE IF NOT EXISTS serialized_timeline_event_revisions (
        id TEXT PRIMARY KEY,
        event_id TEXT NOT NULL,
        series_id TEXT NOT NULL,
        event_revision_number INTEGER NOT NULL,
        timeline_revision_number INTEGER NOT NULL,
        snapshot TEXT NOT NULL,
        change_kind TEXT NOT NULL DEFAULT 'create',
        change_reason TEXT,
        actor TEXT,
        created_at TEXT DEFAULT CURRENT_TIMESTAMP,
        UNIQUE(event_id, event_revision_number),
        FOREIGN KEY (event_id) REFERENCES serialized_timeline_events(id),
        FOREIGN KEY (series_id) REFERENCES serialized_series_bibles(id)
      )`,
      `CREATE TABLE IF NOT EXISTS serialized_timeline_commits (
        id TEXT PRIMARY KEY,
        series_id TEXT NOT NULL,
        timeline_revision_number INTEGER NOT NULL,
        commit_kind TEXT NOT NULL DEFAULT 'append',
        episode_number INTEGER,
        event_ids TEXT NOT NULL DEFAULT '[]',
        reason TEXT,
        actor TEXT,
        created_at TEXT DEFAULT CURRENT_TIMESTAMP,
        UNIQUE(series_id, timeline_revision_number),
        FOREIGN KEY (series_id) REFERENCES serialized_series_bibles(id)
      )`,
