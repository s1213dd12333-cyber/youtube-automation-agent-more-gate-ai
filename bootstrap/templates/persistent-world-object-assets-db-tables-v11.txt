      // Phase 11.10.3 provider-backed canonical visual references for persistent world objects
      `CREATE TABLE IF NOT EXISTS persistent_world_object_assets (
        id TEXT PRIMARY KEY,
        asset_key TEXT NOT NULL UNIQUE,
        object_id TEXT NOT NULL,
        asset_role TEXT NOT NULL DEFAULT 'object_reference',
        version TEXT NOT NULL DEFAULT '11.10.3',
        identity_fingerprint TEXT NOT NULL,
        canonical INTEGER NOT NULL DEFAULT 1,
        status TEXT NOT NULL DEFAULT 'ready',
        asset_path TEXT NOT NULL,
        asset_sha256 TEXT NOT NULL,
        source_kind TEXT NOT NULL DEFAULT 'explicit-canonical-object-reference',
        source_production_id TEXT,
        source_environment_id TEXT,
        source_ref TEXT,
        source_prop_lock_id TEXT,
        provider TEXT,
        model TEXT,
        created_at TEXT NOT NULL,
        updated_at TEXT NOT NULL,
        UNIQUE(object_id, asset_role),
        FOREIGN KEY (object_id) REFERENCES persistent_world_objects(id) ON DELETE CASCADE,
        FOREIGN KEY (source_production_id) REFERENCES productions(id) ON DELETE SET NULL
      )`,
      `CREATE INDEX IF NOT EXISTS idx_persistent_world_object_assets_object ON persistent_world_object_assets(object_id, status, created_at)`,
      `CREATE INDEX IF NOT EXISTS idx_persistent_world_object_assets_origin ON persistent_world_object_assets(source_production_id, provider, created_at)`,
