      // Phase 12.6 — Autonomous Research / Evidence Acquisition
      `CREATE TABLE IF NOT EXISTS newsroom_autonomous_research_policy_revisions (
        id TEXT PRIMARY KEY,
        revision_number INTEGER NOT NULL UNIQUE,
        policy_json TEXT NOT NULL,
        policy_fingerprint TEXT NOT NULL UNIQUE,
        reason TEXT NOT NULL,
        created_by TEXT NOT NULL,
        created_at TEXT DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE TABLE IF NOT EXISTS newsroom_autonomous_research_runs (
        id TEXT PRIMARY KEY,
        plan_id TEXT,
        decision_id TEXT,
        cluster_id TEXT,
        event_id TEXT,
        scan_id TEXT,
        engine_version TEXT NOT NULL,
        status TEXT NOT NULL CHECK(status IN ('RESEARCH_MORE','EVIDENCE_READY','BLOCK')),
        round_count INTEGER NOT NULL DEFAULT 0,
        requirements_json TEXT NOT NULL DEFAULT '{}',
        questions_json TEXT NOT NULL DEFAULT '[]',
        sources_json TEXT NOT NULL DEFAULT '[]',
        source_domains_json TEXT NOT NULL DEFAULT '[]',
        findings_json TEXT NOT NULL DEFAULT '[]',
        gaps_json TEXT NOT NULL DEFAULT '[]',
        adapter_status_json TEXT NOT NULL DEFAULT '{}',
        evidence_score INTEGER NOT NULL DEFAULT 0,
        coverage_ratio REAL NOT NULL DEFAULT 0,
        primary_source_count INTEGER NOT NULL DEFAULT 0,
        independent_source_count INTEGER NOT NULL DEFAULT 0,
        promoted_idea_id TEXT,
        assignment_id TEXT,
        run_fingerprint TEXT NOT NULL UNIQUE,
        created_at TEXT DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE INDEX IF NOT EXISTS idx_newsroom_research_plan ON newsroom_autonomous_research_runs(plan_id, created_at)`,
      `CREATE INDEX IF NOT EXISTS idx_newsroom_research_status ON newsroom_autonomous_research_runs(status, created_at)`,
      `CREATE TABLE IF NOT EXISTS newsroom_autonomous_research_rounds (
        id TEXT PRIMARY KEY,
        run_id TEXT NOT NULL,
        round_number INTEGER NOT NULL,
        status TEXT NOT NULL CHECK(status IN ('RESEARCH_MORE','EVIDENCE_READY','BLOCK')),
        queries_json TEXT NOT NULL DEFAULT '[]',
        source_count INTEGER NOT NULL DEFAULT 0,
        independent_source_count INTEGER NOT NULL DEFAULT 0,
        primary_source_count INTEGER NOT NULL DEFAULT 0,
        coverage_ratio REAL NOT NULL DEFAULT 0,
        gaps_json TEXT NOT NULL DEFAULT '[]',
        created_at TEXT DEFAULT CURRENT_TIMESTAMP,
        UNIQUE(run_id, round_number)
      )`,
      `CREATE INDEX IF NOT EXISTS idx_newsroom_research_round_run ON newsroom_autonomous_research_rounds(run_id, round_number)`,
