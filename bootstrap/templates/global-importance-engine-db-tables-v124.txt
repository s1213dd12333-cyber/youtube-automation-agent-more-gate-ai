      // Phase 12.4 — Global Importance Engine: versioned neutral policy and immutable structural-impact assessments.
      `CREATE TABLE IF NOT EXISTS global_news_importance_policy_revisions (
        id TEXT PRIMARY KEY,
        revision_number INTEGER NOT NULL UNIQUE,
        policy_json TEXT NOT NULL,
        policy_fingerprint TEXT NOT NULL,
        reason TEXT NOT NULL,
        created_by TEXT NOT NULL,
        created_at TEXT DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE INDEX IF NOT EXISTS idx_global_news_importance_policy_revision ON global_news_importance_policy_revisions(revision_number DESC)`,
      `CREATE TABLE IF NOT EXISTS global_news_importance_assessments (
        id TEXT PRIMARY KEY,
        scan_id TEXT,
        event_id TEXT,
        cluster_id TEXT NOT NULL,
        event_revision INTEGER NOT NULL DEFAULT 0,
        engine_version TEXT NOT NULL,
        importance_score INTEGER NOT NULL CHECK(importance_score BETWEEN 0 AND 100),
        impact_tier TEXT NOT NULL CHECK(impact_tier IN ('limited','regional','international','global','systemic')),
        confidence_score INTEGER NOT NULL CHECK(confidence_score BETWEEN 0 AND 100),
        dimensions_json TEXT NOT NULL DEFAULT '{}',
        evidence_json TEXT NOT NULL DEFAULT '{}',
        reason_codes_json TEXT NOT NULL DEFAULT '[]',
        rationale TEXT NOT NULL,
        policy_revision INTEGER NOT NULL DEFAULT 0,
        assessment_fingerprint TEXT NOT NULL UNIQUE,
        created_at TEXT DEFAULT CURRENT_TIMESTAMP,
        FOREIGN KEY (scan_id) REFERENCES global_news_scans(id),
        FOREIGN KEY (event_id) REFERENCES global_news_events(id),
        FOREIGN KEY (cluster_id) REFERENCES global_news_clusters(id)
      )`,
      `CREATE INDEX IF NOT EXISTS idx_global_news_importance_recent ON global_news_importance_assessments(created_at DESC, importance_score DESC)`,
      `CREATE INDEX IF NOT EXISTS idx_global_news_importance_event ON global_news_importance_assessments(event_id, created_at DESC)`,
      `CREATE INDEX IF NOT EXISTS idx_global_news_importance_tier ON global_news_importance_assessments(impact_tier, importance_score DESC)`,
