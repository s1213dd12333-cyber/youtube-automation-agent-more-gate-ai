      // Phase 11.12.3 serialized narrative Episode Memory / Ledger
      `CREATE TABLE IF NOT EXISTS serialized_episode_memories (
        id TEXT PRIMARY KEY,
        series_id TEXT NOT NULL,
        episode_number INTEGER NOT NULL,
        strategy_id TEXT,
        production_id TEXT,
        title TEXT NOT NULL DEFAULT '',
        summary TEXT NOT NULL DEFAULT '',
        discoveries TEXT NOT NULL DEFAULT '[]',
        established_facts TEXT NOT NULL DEFAULT '[]',
        resolved_questions TEXT NOT NULL DEFAULT '[]',
        unresolved_questions TEXT NOT NULL DEFAULT '[]',
        narrative_promises TEXT NOT NULL DEFAULT '[]',
        cliffhangers TEXT NOT NULL DEFAULT '[]',
        timeline_event_ids TEXT NOT NULL DEFAULT '[]',
        approval_id TEXT NOT NULL,
        approved_by TEXT NOT NULL,
        approval_reason TEXT NOT NULL,
        approved_at TEXT NOT NULL,
        source_fingerprint TEXT,
        revision_number INTEGER NOT NULL DEFAULT 1,
        status TEXT NOT NULL DEFAULT 'finalized',
        created_at TEXT NOT NULL,
        updated_at TEXT NOT NULL,
        UNIQUE(series_id, episode_number),
        FOREIGN KEY (series_id) REFERENCES serialized_series_bibles(id) ON DELETE CASCADE
      )`,
      `CREATE INDEX IF NOT EXISTS idx_serialized_episode_memories_series ON serialized_episode_memories(series_id, episode_number DESC)`,
      `CREATE INDEX IF NOT EXISTS idx_serialized_episode_memories_strategy ON serialized_episode_memories(strategy_id)`,
      `CREATE INDEX IF NOT EXISTS idx_serialized_episode_memories_production ON serialized_episode_memories(production_id)`,
      `CREATE TABLE IF NOT EXISTS serialized_episode_memory_revisions (
        id TEXT PRIMARY KEY,
        episode_memory_id TEXT NOT NULL,
        series_id TEXT NOT NULL,
        episode_number INTEGER NOT NULL,
        revision_number INTEGER NOT NULL,
        snapshot TEXT NOT NULL DEFAULT '{}',
        change_kind TEXT NOT NULL DEFAULT 'finalize',
        change_reason TEXT NOT NULL DEFAULT '',
        actor TEXT,
        created_at TEXT NOT NULL,
        UNIQUE(episode_memory_id, revision_number),
        FOREIGN KEY (episode_memory_id) REFERENCES serialized_episode_memories(id) ON DELETE CASCADE,
        FOREIGN KEY (series_id) REFERENCES serialized_series_bibles(id) ON DELETE CASCADE
      )`,
      `CREATE INDEX IF NOT EXISTS idx_serialized_episode_memory_revisions_episode ON serialized_episode_memory_revisions(episode_memory_id, revision_number DESC)`,
      `CREATE TABLE IF NOT EXISTS serialized_episode_memory_commits (
        id TEXT PRIMARY KEY,
        series_id TEXT NOT NULL,
        episode_memory_id TEXT NOT NULL,
        episode_number INTEGER NOT NULL,
        memory_revision_number INTEGER NOT NULL,
        bible_revision_number INTEGER NOT NULL,
        commit_kind TEXT NOT NULL DEFAULT 'finalize',
        timeline_event_ids TEXT NOT NULL DEFAULT '[]',
        reason TEXT NOT NULL DEFAULT '',
        actor TEXT,
        created_at TEXT NOT NULL,
        FOREIGN KEY (series_id) REFERENCES serialized_series_bibles(id) ON DELETE CASCADE,
        FOREIGN KEY (episode_memory_id) REFERENCES serialized_episode_memories(id) ON DELETE CASCADE
      )`,
      `CREATE INDEX IF NOT EXISTS idx_serialized_episode_memory_commits_series ON serialized_episode_memory_commits(series_id, episode_number DESC, created_at DESC)`,
