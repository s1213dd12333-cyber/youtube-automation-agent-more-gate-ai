      `CREATE TABLE IF NOT EXISTS newsroom_self_improvement_observations (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        observation_id TEXT NOT NULL UNIQUE,
        source TEXT NOT NULL,
        category TEXT NOT NULL,
        severity TEXT NOT NULL CHECK(severity IN ('low','medium','high','critical')),
        fingerprint TEXT NOT NULL,
        summary TEXT NOT NULL,
        evidence_json TEXT NOT NULL DEFAULT '[]',
        context_json TEXT NOT NULL DEFAULT '{}',
        created_at DATETIME DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE INDEX IF NOT EXISTS idx_self_improvement_observations_fingerprint ON newsroom_self_improvement_observations(fingerprint, created_at DESC)`,
      `CREATE TABLE IF NOT EXISTS newsroom_self_improvement_proposals (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        proposal_id TEXT NOT NULL UNIQUE,
        kind TEXT NOT NULL CHECK(kind IN ('prompt','rule','code')),
        status TEXT NOT NULL DEFAULT 'proposed' CHECK(status IN ('proposed','approved','rejected','pr_open','merged','closed')),
        title TEXT NOT NULL,
        rationale TEXT NOT NULL,
        target_json TEXT NOT NULL DEFAULT '{}',
        change_json TEXT NOT NULL DEFAULT '{}',
        evidence_json TEXT NOT NULL DEFAULT '[]',
        confidence REAL NOT NULL DEFAULT 0,
        risk TEXT NOT NULL DEFAULT 'medium' CHECK(risk IN ('low','medium','high')),
        requires_human_approval INTEGER NOT NULL DEFAULT 1,
        approved_by TEXT,
        approved_at DATETIME,
        github_branch TEXT,
        github_pr_number INTEGER,
        github_pr_url TEXT,
        created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
        updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE INDEX IF NOT EXISTS idx_self_improvement_proposals_status ON newsroom_self_improvement_proposals(status, kind, created_at DESC)`,
      `CREATE TABLE IF NOT EXISTS newsroom_self_improvement_runs (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        run_id TEXT NOT NULL UNIQUE,
        proposal_id TEXT NOT NULL,
        action TEXT NOT NULL,
        status TEXT NOT NULL,
        details_json TEXT NOT NULL DEFAULT '{}',
        created_at DATETIME DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE INDEX IF NOT EXISTS idx_self_improvement_runs_proposal ON newsroom_self_improvement_runs(proposal_id, created_at DESC)`,
