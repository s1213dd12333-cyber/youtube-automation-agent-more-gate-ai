      `CREATE TABLE IF NOT EXISTS newsroom_performance_learning_snapshots (
        id TEXT PRIMARY KEY,
        engine_version TEXT NOT NULL,
        video_id TEXT NOT NULL,
        production_id TEXT,
        measurement_window TEXT NOT NULL,
        published_at DATETIME,
        measured_at DATETIME NOT NULL,
        confidence TEXT NOT NULL CHECK(confidence IN ('low','medium','high')),
        simulated INTEGER NOT NULL DEFAULT 0,
        eligible INTEGER NOT NULL DEFAULT 0,
        views INTEGER NOT NULL DEFAULT 0,
        impressions INTEGER NOT NULL DEFAULT 0,
        ctr REAL NOT NULL DEFAULT 0,
        retention REAL NOT NULL DEFAULT 0,
        comments INTEGER NOT NULL DEFAULT 0,
        comments_per_thousand_views REAL NOT NULL DEFAULT 0,
        views_per_hour REAL NOT NULL DEFAULT 0,
        metrics_json TEXT NOT NULL,
        dimensions_json TEXT NOT NULL,
        snapshot_fingerprint TEXT NOT NULL UNIQUE,
        created_at DATETIME DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE INDEX IF NOT EXISTS idx_performance_learning_video ON newsroom_performance_learning_snapshots(video_id, measurement_window, measured_at DESC)`,
      `CREATE INDEX IF NOT EXISTS idx_performance_learning_confidence ON newsroom_performance_learning_snapshots(eligible, confidence, measured_at DESC)`,
      `CREATE TABLE IF NOT EXISTS newsroom_performance_learning_signals (
        id TEXT PRIMARY KEY,
        engine_version TEXT NOT NULL,
        signal_type TEXT NOT NULL CHECK(signal_type IN ('subject','pillar','format')),
        signal_key TEXT NOT NULL,
        sample_count INTEGER NOT NULL DEFAULT 0,
        total_views INTEGER NOT NULL DEFAULT 0,
        total_impressions INTEGER NOT NULL DEFAULT 0,
        average_ctr REAL NOT NULL DEFAULT 0,
        average_retention REAL NOT NULL DEFAULT 0,
        average_comments_per_thousand_views REAL NOT NULL DEFAULT 0,
        average_views_per_hour REAL NOT NULL DEFAULT 0,
        performance_index REAL NOT NULL DEFAULT 0,
        priority_adjustment INTEGER NOT NULL DEFAULT 0,
        confidence TEXT NOT NULL CHECK(confidence IN ('medium','high')),
        evidence_json TEXT NOT NULL,
        signal_fingerprint TEXT NOT NULL,
        updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
        UNIQUE(signal_type, signal_key)
      )`,
      `CREATE INDEX IF NOT EXISTS idx_performance_learning_signal_priority ON newsroom_performance_learning_signals(signal_type, priority_adjustment DESC, confidence)`,
      `CREATE TABLE IF NOT EXISTS newsroom_performance_learning_applications (
        id TEXT PRIMARY KEY,
        engine_version TEXT NOT NULL,
        target_type TEXT NOT NULL,
        target_id TEXT NOT NULL,
        applied_adjustment INTEGER NOT NULL DEFAULT 0,
        signal_json TEXT NOT NULL,
        application_fingerprint TEXT NOT NULL UNIQUE,
        created_at DATETIME DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE INDEX IF NOT EXISTS idx_performance_learning_applications_target ON newsroom_performance_learning_applications(target_type, target_id, created_at DESC)`,
