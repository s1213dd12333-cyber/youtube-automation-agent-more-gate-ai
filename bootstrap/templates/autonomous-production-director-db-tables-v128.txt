      `CREATE TABLE IF NOT EXISTS newsroom_production_directives (
        id TEXT PRIMARY KEY,
        plan_id TEXT,
        claim_packet_id TEXT,
        decision_id TEXT,
        cluster_id TEXT,
        engine_version TEXT NOT NULL,
        status TEXT NOT NULL CHECK(status IN ('PLAN_READY','PLAN_BLOCKED')),
        mode TEXT NOT NULL CHECK(mode IN ('speed_first','balanced','depth_first')),
        format TEXT NOT NULL,
        urgency TEXT NOT NULL,
        visual_strategy TEXT NOT NULL CHECK(visual_strategy IN ('source_first','hybrid','evidence_first_hybrid')),
        provider_tier TEXT NOT NULL CHECK(provider_tier IN ('local_first','standard','premium_allowed')),
        max_budget_usd REAL NOT NULL DEFAULT 0,
        target_duration_seconds INTEGER NOT NULL,
        scene_seconds INTEGER NOT NULL,
        scene_count INTEGER NOT NULL,
        tts_json TEXT NOT NULL DEFAULT '{}',
        visuals_json TEXT NOT NULL DEFAULT '{}',
        fact_locks_json TEXT NOT NULL DEFAULT '[]',
        forbidden_json TEXT NOT NULL DEFAULT '[]',
        directive_fingerprint TEXT NOT NULL UNIQUE,
        assignment_id TEXT,
        created_at TEXT DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE INDEX IF NOT EXISTS idx_newsroom_production_plan ON newsroom_production_directives(plan_id, created_at)`,
      `CREATE INDEX IF NOT EXISTS idx_newsroom_production_claim_packet ON newsroom_production_directives(claim_packet_id, created_at)`,
      `CREATE INDEX IF NOT EXISTS idx_newsroom_production_assignment ON newsroom_production_directives(assignment_id, created_at)`,
