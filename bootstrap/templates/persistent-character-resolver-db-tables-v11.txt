      // Phase 11.11.2 persistent character aliases and audited resolver decisions
      `CREATE TABLE IF NOT EXISTS persistent_character_aliases (
        id TEXT PRIMARY KEY,
        namespace TEXT NOT NULL DEFAULT 'default',
        character_id TEXT NOT NULL,
        alias_text TEXT NOT NULL,
        alias_key TEXT NOT NULL,
        canonical INTEGER NOT NULL DEFAULT 0,
        source_kind TEXT NOT NULL DEFAULT 'character_registration',
        created_at TEXT NOT NULL,
        updated_at TEXT NOT NULL,
        UNIQUE(namespace, character_id, alias_key),
        FOREIGN KEY (character_id) REFERENCES persistent_characters(id) ON DELETE CASCADE
      )`,
      `CREATE INDEX IF NOT EXISTS idx_persistent_character_aliases_key ON persistent_character_aliases(namespace, alias_key, character_id)`,
      `CREATE TABLE IF NOT EXISTS persistent_character_resolutions (
        id TEXT PRIMARY KEY,
        namespace TEXT NOT NULL DEFAULT 'default',
        production_id TEXT NOT NULL,
        scope_key TEXT NOT NULL,
        reference_text TEXT NOT NULL DEFAULT '',
        reference_key TEXT NOT NULL DEFAULT '',
        requested_species TEXT,
        status TEXT NOT NULL,
        character_id TEXT,
        match_mode TEXT,
        confidence REAL NOT NULL DEFAULT 0,
        candidate_character_ids TEXT NOT NULL DEFAULT '[]',
        reason TEXT,
        identity_fingerprint TEXT,
        created_at TEXT NOT NULL,
        updated_at TEXT NOT NULL,
        UNIQUE(production_id, scope_key, reference_key),
        FOREIGN KEY (production_id) REFERENCES productions(id) ON DELETE CASCADE,
        FOREIGN KEY (character_id) REFERENCES persistent_characters(id) ON DELETE SET NULL
      )`,
      `CREATE INDEX IF NOT EXISTS idx_persistent_character_resolutions_prod ON persistent_character_resolutions(production_id, status, updated_at)`,
      `CREATE INDEX IF NOT EXISTS idx_persistent_character_resolutions_character ON persistent_character_resolutions(character_id, match_mode, updated_at)`,
