      // Phase 11.10.6 cross-video persistent object continuity gate audit trail
      `CREATE TABLE IF NOT EXISTS cross_video_object_continuity_checks (
        id TEXT PRIMARY KEY,
        production_id TEXT NOT NULL,
        scene_id TEXT NOT NULL,
        shot_id TEXT NOT NULL,
        keyframe_id TEXT NOT NULL,
        binding_id TEXT,
        object_id TEXT,
        canonical_asset_id TEXT,
        canonical_asset_path TEXT,
        canonical_asset_sha256 TEXT,
        canonical_source_production_id TEXT,
        current_production_id TEXT NOT NULL,
        reused_across_videos INTEGER NOT NULL DEFAULT 0,
        state_id TEXT,
        state_fingerprint TEXT,
        version TEXT NOT NULL DEFAULT '11.10.6',
        attempt INTEGER NOT NULL DEFAULT 0,
        status TEXT NOT NULL,
        accepted INTEGER NOT NULL DEFAULT 0,
        required_binding INTEGER NOT NULL DEFAULT 1,
        visibility TEXT,
        provider_used INTEGER NOT NULL DEFAULT 0,
        provider_mode TEXT,
        model TEXT,
        vision_confidence REAL NOT NULL DEFAULT 0,
        object_present INTEGER,
        same_canonical_object INTEGER,
        canonical_identity_consistent INTEGER,
        state_consistent INTEGER,
        reasons TEXT NOT NULL DEFAULT '[]',
        identity_mismatches TEXT NOT NULL DEFAULT '[]',
        prompt_fingerprint TEXT,
        response_fingerprint TEXT,
        notes TEXT,
        created_at TEXT NOT NULL,
        FOREIGN KEY (production_id) REFERENCES productions(id) ON DELETE CASCADE,
        FOREIGN KEY (scene_id) REFERENCES production_scenes(id) ON DELETE CASCADE,
        FOREIGN KEY (shot_id) REFERENCES scene_shots(id) ON DELETE CASCADE,
        FOREIGN KEY (keyframe_id) REFERENCES shot_keyframes(id) ON DELETE CASCADE,
        FOREIGN KEY (binding_id) REFERENCES persistent_world_object_bindings(id) ON DELETE SET NULL,
        FOREIGN KEY (object_id) REFERENCES persistent_world_objects(id) ON DELETE SET NULL,
        FOREIGN KEY (canonical_asset_id) REFERENCES persistent_world_object_assets(id) ON DELETE SET NULL,
        FOREIGN KEY (state_id) REFERENCES persistent_world_object_states(id) ON DELETE SET NULL
      )`,
      `CREATE INDEX IF NOT EXISTS idx_cross_video_object_continuity_prod ON cross_video_object_continuity_checks(production_id, scene_id, keyframe_id, attempt, created_at)`,
      `CREATE INDEX IF NOT EXISTS idx_cross_video_object_continuity_object ON cross_video_object_continuity_checks(object_id, reused_across_videos, accepted, created_at)`,
