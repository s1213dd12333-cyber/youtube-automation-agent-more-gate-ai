      // Phase 11.11.3 provider-backed canonical visual references for persistent characters
      `CREATE TABLE IF NOT EXISTS persistent_character_assets (
        id TEXT PRIMARY KEY,
        asset_key TEXT NOT NULL UNIQUE,
        character_id TEXT NOT NULL,
        asset_role TEXT NOT NULL DEFAULT 'character_reference',
        version TEXT NOT NULL DEFAULT '11.11.3',
        identity_fingerprint TEXT NOT NULL,
        canonical INTEGER NOT NULL DEFAULT 1,
        status TEXT NOT NULL DEFAULT 'ready',
        asset_path TEXT NOT NULL,
        asset_sha256 TEXT NOT NULL,
        source_kind TEXT NOT NULL DEFAULT 'explicit-canonical-character-reference',
        source_production_id TEXT,
        source_bible_id TEXT,
        source_character_id TEXT,
        source_ref TEXT,
        provider TEXT,
        model TEXT,
        created_at TEXT NOT NULL,
        updated_at TEXT NOT NULL,
        UNIQUE(character_id, asset_role),
        FOREIGN KEY (character_id) REFERENCES persistent_characters(id) ON DELETE CASCADE,
        FOREIGN KEY (source_production_id) REFERENCES productions(id) ON DELETE SET NULL
      )`,
      `CREATE INDEX IF NOT EXISTS idx_persistent_character_assets_character ON persistent_character_assets(character_id, status, created_at)`,
      `CREATE INDEX IF NOT EXISTS idx_persistent_character_assets_origin ON persistent_character_assets(source_production_id, provider, created_at)`,
