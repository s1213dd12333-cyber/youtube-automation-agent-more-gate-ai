      // Phase 12.7 — Evidence / Truth Engine
      `CREATE TABLE IF NOT EXISTS newsroom_claim_verification_packets (
        id TEXT PRIMARY KEY,
        research_run_id TEXT NOT NULL,
        plan_id TEXT,
        decision_id TEXT,
        cluster_id TEXT,
        engine_version TEXT NOT NULL,
        status TEXT NOT NULL CHECK(status IN ('VERIFIED','NEEDS_RESEARCH','BLOCK')),
        claim_count INTEGER NOT NULL DEFAULT 0,
        classification_counts_json TEXT NOT NULL DEFAULT '{}',
        unresolved_count INTEGER NOT NULL DEFAULT 0,
        blocking_count INTEGER NOT NULL DEFAULT 0,
        confidence_score INTEGER NOT NULL DEFAULT 0,
        packet_fingerprint TEXT NOT NULL UNIQUE,
        created_at TEXT DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE INDEX IF NOT EXISTS idx_newsroom_claim_packet_run ON newsroom_claim_verification_packets(research_run_id, created_at)`,
      `CREATE INDEX IF NOT EXISTS idx_newsroom_claim_packet_status ON newsroom_claim_verification_packets(status, created_at)`,
      `CREATE TABLE IF NOT EXISTS newsroom_claim_verification_claims (
        id TEXT PRIMARY KEY,
        packet_id TEXT NOT NULL,
        ordinal INTEGER NOT NULL,
        question TEXT NOT NULL,
        claim_text TEXT NOT NULL,
        classification TEXT NOT NULL CHECK(classification IN ('confirmed','reported','claimed','disputed','unverified','false','unknown')),
        confidence_score INTEGER NOT NULL DEFAULT 0,
        reason TEXT NOT NULL,
        support_domains_json TEXT NOT NULL DEFAULT '[]',
        contradiction_domains_json TEXT NOT NULL DEFAULT '[]',
        supporting_sources_json TEXT NOT NULL DEFAULT '[]',
        contradicting_sources_json TEXT NOT NULL DEFAULT '[]',
        evidence_excerpt_json TEXT NOT NULL DEFAULT '[]',
        claim_fingerprint TEXT NOT NULL,
        created_at TEXT DEFAULT CURRENT_TIMESTAMP,
        UNIQUE(packet_id, ordinal),
        FOREIGN KEY (packet_id) REFERENCES newsroom_claim_verification_packets(id)
      )`,
      `CREATE INDEX IF NOT EXISTS idx_newsroom_claim_packet_claims ON newsroom_claim_verification_claims(packet_id, ordinal)`,
