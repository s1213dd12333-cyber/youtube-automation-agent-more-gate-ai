      // Phase 11.10.5 explicit scene/shot bindings for persistent world objects
      `CREATE TABLE IF NOT EXISTS persistent_world_object_bindings (
        id TEXT PRIMARY KEY,
        production_id TEXT NOT NULL,
        scene_id TEXT NOT NULL,
        shot_id TEXT NOT NULL,
        shot_index INTEGER NOT NULL DEFAULT 0,
        object_id TEXT,
        object_key TEXT,
        version TEXT NOT NULL DEFAULT '11.10.5',
        status TEXT NOT NULL,
        reference_text TEXT,
        reference_key TEXT,
        match_mode TEXT,
        confidence REAL NOT NULL DEFAULT 0,
        visibility TEXT NOT NULL DEFAULT 'visible',
        interaction TEXT NOT NULL DEFAULT 'none',
        holder_key TEXT,
        placement TEXT,
        required INTEGER NOT NULL DEFAULT 1,
        state_id TEXT,
        state_fingerprint TEXT,
        canonical_asset_id TEXT,
        canonical_asset_sha256 TEXT,
        binding_fingerprint TEXT NOT NULL,
        prompt_fragment TEXT NOT NULL DEFAULT '',
        source_kind TEXT NOT NULL DEFAULT 'scene_object_binding',
        source_ref TEXT,
        reason TEXT,
        created_at TEXT NOT NULL,
        updated_at TEXT NOT NULL,
        UNIQUE(production_id, scene_id, shot_id, id),
        FOREIGN KEY (production_id) REFERENCES productions(id) ON DELETE CASCADE,
        FOREIGN KEY (scene_id) REFERENCES production_scenes(id) ON DELETE CASCADE,
        FOREIGN KEY (object_id) REFERENCES persistent_world_objects(id) ON DELETE SET NULL,
        FOREIGN KEY (state_id) REFERENCES persistent_world_object_states(id) ON DELETE SET NULL,
        FOREIGN KEY (canonical_asset_id) REFERENCES persistent_world_object_assets(id) ON DELETE SET NULL
      )`,
      `CREATE INDEX IF NOT EXISTS idx_world_object_bindings_prod ON persistent_world_object_bindings(production_id, scene_id, shot_index)`,
      `CREATE INDEX IF NOT EXISTS idx_world_object_bindings_object ON persistent_world_object_bindings(object_id, production_id, scene_id)`,
