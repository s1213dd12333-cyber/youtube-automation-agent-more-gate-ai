      // Phase 12.1 — Global News Radar + Autonomous Editor audit/persistence.
      `CREATE TABLE IF NOT EXISTS global_news_scans (
        id TEXT PRIMARY KEY,
        status TEXT NOT NULL DEFAULT 'running' CHECK(status IN ('running','completed','partial','failed','skipped')),
        started_at TEXT NOT NULL,
        completed_at TEXT,
        config_json TEXT NOT NULL DEFAULT '{}',
        source_results_json TEXT NOT NULL DEFAULT '[]',
        article_count INTEGER NOT NULL DEFAULT 0,
        cluster_count INTEGER NOT NULL DEFAULT 0,
        actionable_count INTEGER NOT NULL DEFAULT 0,
        error TEXT
      )`,
      `CREATE TABLE IF NOT EXISTS global_news_articles (
        id TEXT PRIMARY KEY,
        url_hash TEXT NOT NULL UNIQUE,
        url TEXT NOT NULL,
        title TEXT NOT NULL,
        normalized_title TEXT NOT NULL,
        summary TEXT,
        source_name TEXT,
        source_domain TEXT,
        source_country TEXT,
        source_region TEXT,
        language TEXT,
        adapter TEXT NOT NULL,
        lane_id TEXT,
        published_at TEXT,
        first_seen_at TEXT DEFAULT CURRENT_TIMESTAMP,
        last_seen_at TEXT DEFAULT CURRENT_TIMESTAMP,
        last_scan_id TEXT,
        metadata_json TEXT NOT NULL DEFAULT '{}',
        FOREIGN KEY (last_scan_id) REFERENCES global_news_scans(id)
      )`,
      `CREATE INDEX IF NOT EXISTS idx_global_news_articles_published ON global_news_articles(published_at DESC)`,
      `CREATE INDEX IF NOT EXISTS idx_global_news_articles_source ON global_news_articles(source_domain, last_seen_at DESC)`,
      `CREATE TABLE IF NOT EXISTS global_news_clusters (
        id TEXT PRIMARY KEY,
        cluster_key TEXT NOT NULL,
        canonical_title TEXT NOT NULL,
        status TEXT NOT NULL DEFAULT 'active' CHECK(status IN ('active','assigned','covered','ignored','stale')),
        first_seen_at TEXT NOT NULL,
        last_seen_at TEXT DEFAULT CURRENT_TIMESTAMP,
        last_material_change TEXT,
        global_score INTEGER NOT NULL DEFAULT 0 CHECK(global_score BETWEEN 0 AND 100),
        confidence_score INTEGER NOT NULL DEFAULT 0 CHECK(confidence_score BETWEEN 0 AND 100),
        velocity_score INTEGER NOT NULL DEFAULT 0 CHECK(velocity_score BETWEEN 0 AND 100),
        freshness_score INTEGER NOT NULL DEFAULT 0 CHECK(freshness_score BETWEEN 0 AND 100),
        geography_score INTEGER NOT NULL DEFAULT 0 CHECK(geography_score BETWEEN 0 AND 100),
        source_breadth_score INTEGER NOT NULL DEFAULT 0 CHECK(source_breadth_score BETWEEN 0 AND 100),
        volume_score INTEGER NOT NULL DEFAULT 0 CHECK(volume_score BETWEEN 0 AND 100),
        source_count INTEGER NOT NULL DEFAULT 0,
        region_count INTEGER NOT NULL DEFAULT 0,
        article_count INTEGER NOT NULL DEFAULT 0,
        independent_evidence_units INTEGER NOT NULL DEFAULT 0,
        source_keys_json TEXT NOT NULL DEFAULT '[]',
        source_regions_json TEXT NOT NULL DEFAULT '[]',
        article_ids_json TEXT NOT NULL DEFAULT '[]',
        topic_tokens_json TEXT NOT NULL DEFAULT '[]',
        material_fingerprint TEXT NOT NULL,
        last_scan_id TEXT,
        covered_at TEXT,
        coverage_count INTEGER NOT NULL DEFAULT 0,
        updated_at TEXT DEFAULT CURRENT_TIMESTAMP,
        FOREIGN KEY (last_scan_id) REFERENCES global_news_scans(id)
      )`,
      `CREATE INDEX IF NOT EXISTS idx_global_news_clusters_score ON global_news_clusters(global_score DESC, last_seen_at DESC)`,
      `CREATE INDEX IF NOT EXISTS idx_global_news_clusters_status ON global_news_clusters(status, last_seen_at DESC)`,
      `CREATE TABLE IF NOT EXISTS global_news_editor_decisions (
        id TEXT PRIMARY KEY,
        cluster_id TEXT NOT NULL,
        scan_id TEXT NOT NULL,
        action TEXT NOT NULL CHECK(action IN ('COVER','WAIT','IGNORE','UPDATE','BREAKING','FOLLOW_UP')),
        rationale TEXT NOT NULL,
        score_json TEXT NOT NULL DEFAULT '{}',
        evidence_json TEXT NOT NULL DEFAULT '{}',
        material_fingerprint TEXT NOT NULL,
        decision_fingerprint TEXT NOT NULL UNIQUE,
        promoted_idea_id TEXT,
        created_at TEXT DEFAULT CURRENT_TIMESTAMP,
        FOREIGN KEY (cluster_id) REFERENCES global_news_clusters(id),
        FOREIGN KEY (scan_id) REFERENCES global_news_scans(id),
        FOREIGN KEY (promoted_idea_id) REFERENCES content_ideas(id)
      )`,
      `CREATE INDEX IF NOT EXISTS idx_global_news_decisions_recent ON global_news_editor_decisions(created_at DESC)`,
      `CREATE INDEX IF NOT EXISTS idx_global_news_decisions_action ON global_news_editor_decisions(action, created_at DESC)`,
      `CREATE TABLE IF NOT EXISTS global_news_assignments (
        id TEXT PRIMARY KEY,
        decision_id TEXT NOT NULL UNIQUE,
        cluster_id TEXT NOT NULL,
        idea_id TEXT NOT NULL,
        assignment_type TEXT NOT NULL,
        topic TEXT NOT NULL,
        angle TEXT NOT NULL,
        format TEXT NOT NULL DEFAULT 'explainer',
        source_urls_json TEXT NOT NULL DEFAULT '[]',
        source_domains_json TEXT NOT NULL DEFAULT '[]',
        created_at TEXT DEFAULT CURRENT_TIMESTAMP,
        FOREIGN KEY (decision_id) REFERENCES global_news_editor_decisions(id),
        FOREIGN KEY (cluster_id) REFERENCES global_news_clusters(id),
        FOREIGN KEY (idea_id) REFERENCES content_ideas(id)
      )`,
      `CREATE INDEX IF NOT EXISTS idx_global_news_assignments_cluster ON global_news_assignments(cluster_id, created_at DESC)`,
