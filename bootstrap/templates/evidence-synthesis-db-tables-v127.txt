      // Phase 12.7 — Evidence Synthesis / Research Brief AI
      `CREATE TABLE IF NOT EXISTS newsroom_evidence_synthesis_policy_revisions (
        id TEXT PRIMARY KEY,
        revision_number INTEGER NOT NULL UNIQUE,
        policy_json TEXT NOT NULL,
        policy_fingerprint TEXT NOT NULL UNIQUE,
        reason TEXT NOT NULL,
        created_by TEXT NOT NULL,
        created_at TEXT DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE TABLE IF NOT EXISTS newsroom_research_briefs (
        id TEXT PRIMARY KEY,
        research_run_id TEXT NOT NULL,
        plan_id TEXT,
        decision_id TEXT,
        cluster_id TEXT,
        event_id TEXT,
        scan_id TEXT,
        engine_version TEXT NOT NULL,
        status TEXT NOT NULL CHECK(status IN ('SYNTHESIS_READY','BLOCK')),
        summary TEXT NOT NULL,
        key_facts_json TEXT NOT NULL DEFAULT '[]',
        question_answers_json TEXT NOT NULL DEFAULT '[]',
        uncertainties_json TEXT NOT NULL DEFAULT '[]',
        contradictions_json TEXT NOT NULL DEFAULT '[]',
        citations_json TEXT NOT NULL DEFAULT '[]',
        claim_count INTEGER NOT NULL DEFAULT 0,
        corroborated_claim_count INTEGER NOT NULL DEFAULT 0,
        confidence_score INTEGER NOT NULL DEFAULT 0 CHECK(confidence_score BETWEEN 0 AND 100),
        promoted_idea_id TEXT,
        assignment_id TEXT,
        brief_fingerprint TEXT NOT NULL UNIQUE,
        created_at TEXT DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE INDEX IF NOT EXISTS idx_newsroom_briefs_run ON newsroom_research_briefs(research_run_id, created_at)`,
      `CREATE INDEX IF NOT EXISTS idx_newsroom_briefs_status ON newsroom_research_briefs(status, created_at)`,
      `CREATE TABLE IF NOT EXISTS newsroom_research_brief_claims (
        id TEXT PRIMARY KEY,
        brief_id TEXT NOT NULL,
        ordinal INTEGER NOT NULL,
        claim_text TEXT NOT NULL,
        claim_type TEXT NOT NULL CHECK(claim_type IN ('fact','attribution','uncertainty')),
        confidence_score INTEGER NOT NULL CHECK(confidence_score BETWEEN 0 AND 100),
        corroboration_count INTEGER NOT NULL DEFAULT 0,
        source_ids_json TEXT NOT NULL DEFAULT '[]',
        source_urls_json TEXT NOT NULL DEFAULT '[]',
        evidence_excerpts_json TEXT NOT NULL DEFAULT '[]',
        claim_fingerprint TEXT NOT NULL,
        created_at TEXT DEFAULT CURRENT_TIMESTAMP,
        UNIQUE(brief_id, claim_fingerprint),
        FOREIGN KEY (brief_id) REFERENCES newsroom_research_briefs(id)
      )`,
      `CREATE INDEX IF NOT EXISTS idx_newsroom_brief_claims_brief ON newsroom_research_brief_claims(brief_id, ordinal)`,
