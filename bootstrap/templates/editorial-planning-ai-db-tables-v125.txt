      `CREATE TABLE IF NOT EXISTS newsroom_editorial_plan_policy_revisions (
        id TEXT PRIMARY KEY,
        revision_number INTEGER NOT NULL UNIQUE,
        policy_json TEXT NOT NULL,
        policy_fingerprint TEXT NOT NULL,
        reason TEXT NOT NULL,
        created_by TEXT NOT NULL,
        created_at DATETIME DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE TABLE IF NOT EXISTS newsroom_editorial_plans (
        id TEXT PRIMARY KEY,
        scan_id TEXT,
        cluster_id TEXT NOT NULL,
        event_id TEXT,
        event_revision INTEGER NOT NULL DEFAULT 0,
        decision_id TEXT,
        brain_decision_id TEXT,
        importance_assessment_id TEXT,
        planner_version TEXT NOT NULL,
        action TEXT NOT NULL CHECK(action IN ('COVER','BREAKING','UPDATE','FOLLOW_UP')),
        topic TEXT NOT NULL,
        angle TEXT NOT NULL,
        format TEXT NOT NULL CHECK(format IN ('breaking_brief','update_brief','follow_up_explainer','rapid_explainer','standard_explainer','deep_dive')),
        urgency TEXT NOT NULL CHECK(urgency IN ('critical','high','standard','deep')),
        target_duration_seconds INTEGER NOT NULL CHECK(target_duration_seconds >= 120 AND target_duration_seconds <= 1800),
        audience TEXT NOT NULL,
        research_json TEXT NOT NULL,
        visuals_json TEXT NOT NULL,
        handoff_json TEXT NOT NULL,
        rationale TEXT NOT NULL,
        policy_revision INTEGER NOT NULL DEFAULT 0,
        plan_fingerprint TEXT NOT NULL UNIQUE,
        promoted_idea_id TEXT,
        assignment_id TEXT,
        created_at DATETIME DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE INDEX IF NOT EXISTS idx_newsroom_editorial_plans_cluster ON newsroom_editorial_plans(cluster_id, created_at DESC)`,
      `CREATE INDEX IF NOT EXISTS idx_newsroom_editorial_plans_event ON newsroom_editorial_plans(event_id, event_revision, created_at DESC)`,
      `CREATE INDEX IF NOT EXISTS idx_newsroom_editorial_plans_format ON newsroom_editorial_plans(format, created_at DESC)`,
