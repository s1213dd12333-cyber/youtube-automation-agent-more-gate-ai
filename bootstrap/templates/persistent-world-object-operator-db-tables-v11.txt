      // Phase 11.10.7 audited operator corrections for persistent world-object library
      `CREATE TABLE IF NOT EXISTS persistent_world_object_operator_actions (
        id TEXT PRIMARY KEY,
        namespace TEXT NOT NULL DEFAULT 'default',
        action_type TEXT NOT NULL,
        object_id TEXT,
        target_type TEXT NOT NULL,
        target_id TEXT,
        production_id TEXT,
        status TEXT NOT NULL,
        actor TEXT NOT NULL DEFAULT 'operator',
        note TEXT,
        before_value TEXT NOT NULL DEFAULT '{}',
        after_value TEXT NOT NULL DEFAULT '{}',
        reason TEXT,
        created_at TEXT NOT NULL,
        FOREIGN KEY (object_id) REFERENCES persistent_world_objects(id) ON DELETE SET NULL,
        FOREIGN KEY (production_id) REFERENCES productions(id) ON DELETE SET NULL
      )`,
      `CREATE INDEX IF NOT EXISTS idx_world_object_operator_actions_object ON persistent_world_object_operator_actions(namespace, object_id, created_at)`,
      `CREATE INDEX IF NOT EXISTS idx_world_object_operator_actions_target ON persistent_world_object_operator_actions(target_type, target_id, created_at)`,
