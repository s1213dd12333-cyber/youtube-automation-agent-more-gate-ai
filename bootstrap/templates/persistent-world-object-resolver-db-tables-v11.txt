      // Phase 11.10.2 persistent object aliases and audited resolver decisions
      `CREATE TABLE IF NOT EXISTS persistent_world_object_aliases (
        id TEXT PRIMARY KEY,
        namespace TEXT NOT NULL DEFAULT 'default',
        object_id TEXT NOT NULL,
        alias_text TEXT NOT NULL,
        alias_key TEXT NOT NULL,
        canonical INTEGER NOT NULL DEFAULT 0,
        source_kind TEXT NOT NULL DEFAULT 'object_registration',
        created_at TEXT NOT NULL,
        updated_at TEXT NOT NULL,
        UNIQUE(namespace, object_id, alias_key),
        FOREIGN KEY (object_id) REFERENCES persistent_world_objects(id) ON DELETE CASCADE
      )`,
      `CREATE INDEX IF NOT EXISTS idx_persistent_world_object_aliases_key ON persistent_world_object_aliases(namespace, alias_key, object_id)`,
      `CREATE TABLE IF NOT EXISTS persistent_world_object_resolutions (
        id TEXT PRIMARY KEY,
        namespace TEXT NOT NULL DEFAULT 'default',
        production_id TEXT NOT NULL,
        scope_key TEXT NOT NULL,
        reference_text TEXT NOT NULL DEFAULT '',
        reference_key TEXT NOT NULL DEFAULT '',
        requested_type TEXT,
        status TEXT NOT NULL,
        object_id TEXT,
        match_mode TEXT,
        confidence REAL NOT NULL DEFAULT 0,
        candidate_object_ids TEXT NOT NULL DEFAULT '[]',
        reason TEXT,
        identity_fingerprint TEXT,
        created_at TEXT NOT NULL,
        updated_at TEXT NOT NULL,
        UNIQUE(production_id, scope_key, reference_key),
        FOREIGN KEY (production_id) REFERENCES productions(id) ON DELETE CASCADE,
        FOREIGN KEY (object_id) REFERENCES persistent_world_objects(id) ON DELETE SET NULL
      )`,
      `CREATE INDEX IF NOT EXISTS idx_persistent_world_object_resolutions_prod ON persistent_world_object_resolutions(production_id, status, updated_at)`,
      `CREATE INDEX IF NOT EXISTS idx_persistent_world_object_resolutions_object ON persistent_world_object_resolutions(object_id, match_mode, updated_at)`,
