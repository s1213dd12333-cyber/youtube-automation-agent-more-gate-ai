      // Phase 12.3 — Autonomous Editorial Decision Brain: mission policy, competitive planning and immutable audit trail.
      `CREATE TABLE IF NOT EXISTS global_news_editorial_policy_revisions (
        id TEXT PRIMARY KEY,
        revision_number INTEGER NOT NULL UNIQUE,
        policy_json TEXT NOT NULL,
        policy_fingerprint TEXT NOT NULL,
        reason TEXT NOT NULL,
        created_by TEXT NOT NULL,
        created_at TEXT DEFAULT CURRENT_TIMESTAMP
      )`,
      `CREATE INDEX IF NOT EXISTS idx_global_news_editorial_policy_revision ON global_news_editorial_policy_revisions(revision_number DESC)`,
      `CREATE TABLE IF NOT EXISTS global_news_editorial_brain_runs (
        id TEXT PRIMARY KEY,
        scan_id TEXT UNIQUE,
        brain_version TEXT NOT NULL,
        policy_revision INTEGER NOT NULL DEFAULT 0,
        policy_json TEXT NOT NULL,
        selected_count INTEGER NOT NULL DEFAULT 0,
        cooldown_active INTEGER NOT NULL DEFAULT 0 CHECK(cooldown_active IN (0,1)),
        next_eligible_at TEXT,
        run_fingerprint TEXT NOT NULL UNIQUE,
        created_at TEXT DEFAULT CURRENT_TIMESTAMP,
        FOREIGN KEY (scan_id) REFERENCES global_news_scans(id)
      )`,
      `CREATE INDEX IF NOT EXISTS idx_global_news_editorial_brain_runs_recent ON global_news_editorial_brain_runs(created_at DESC)`,
      `CREATE TABLE IF NOT EXISTS global_news_editorial_brain_decisions (
        id TEXT PRIMARY KEY,
        run_id TEXT NOT NULL,
        scan_id TEXT,
        event_id TEXT,
        cluster_id TEXT NOT NULL,
        action TEXT NOT NULL CHECK(action IN ('COVER','WAIT','IGNORE','UPDATE','BREAKING','FOLLOW_UP','DEFER')),
        legacy_action TEXT NOT NULL CHECK(legacy_action IN ('COVER','WAIT','IGNORE','UPDATE','BREAKING','FOLLOW_UP')),
        selected INTEGER NOT NULL DEFAULT 0 CHECK(selected IN (0,1)),
        priority_score INTEGER NOT NULL CHECK(priority_score BETWEEN 0 AND 100),
        urgency TEXT NOT NULL CHECK(urgency IN ('critical','high','standard','monitor','low')),
        recommended_format TEXT NOT NULL,
        target_length_minutes INTEGER NOT NULL DEFAULT 8,
        rationale TEXT NOT NULL,
        reason_codes_json TEXT NOT NULL DEFAULT '[]',
        signal_json TEXT NOT NULL DEFAULT '{}',
        policy_revision INTEGER NOT NULL DEFAULT 0,
        decision_fingerprint TEXT NOT NULL UNIQUE,
        next_eligible_at TEXT,
        created_at TEXT DEFAULT CURRENT_TIMESTAMP,
        FOREIGN KEY (run_id) REFERENCES global_news_editorial_brain_runs(id),
        FOREIGN KEY (scan_id) REFERENCES global_news_scans(id),
        FOREIGN KEY (event_id) REFERENCES global_news_events(id),
        FOREIGN KEY (cluster_id) REFERENCES global_news_clusters(id)
      )`,
      `CREATE INDEX IF NOT EXISTS idx_global_news_editorial_brain_decisions_recent ON global_news_editorial_brain_decisions(created_at DESC, priority_score DESC)`,
      `CREATE INDEX IF NOT EXISTS idx_global_news_editorial_brain_decisions_event ON global_news_editorial_brain_decisions(event_id, created_at DESC)`,
      `CREATE INDEX IF NOT EXISTS idx_global_news_editorial_brain_decisions_selected ON global_news_editorial_brain_decisions(selected, created_at DESC)`,
