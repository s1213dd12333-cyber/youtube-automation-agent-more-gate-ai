      // Phase 11.10.4 mutable state/lifecycle overlays for persistent world objects
      `CREATE TABLE IF NOT EXISTS persistent_world_object_states (
        id TEXT PRIMARY KEY,
        object_id TEXT NOT NULL,
        production_id TEXT NOT NULL,
        scene_id TEXT,
        scope_key TEXT NOT NULL,
        version TEXT NOT NULL DEFAULT '11.10.4',
        status TEXT NOT NULL DEFAULT 'neutral',
        persistence TEXT NOT NULL DEFAULT 'scene',
        state_fingerprint TEXT NOT NULL,
        condition TEXT,
        damage TEXT NOT NULL DEFAULT '[]',
        cleanliness TEXT,
        openness TEXT,
        operational_state TEXT,
        contents TEXT NOT NULL DEFAULT '[]',
        holder_key TEXT,
        placement TEXT,
        story_state TEXT NOT NULL DEFAULT '[]',
        conflicts TEXT NOT NULL DEFAULT '[]',
        source_kind TEXT NOT NULL DEFAULT 'explicit_object_state',
        source_ref TEXT,
        parent_state_id TEXT,
        inherited INTEGER NOT NULL DEFAULT 0,
        canonical_identity_fingerprint TEXT,
        created_at TEXT NOT NULL,
        updated_at TEXT NOT NULL,
        UNIQUE(production_id, object_id, scope_key),
        FOREIGN KEY (object_id) REFERENCES persistent_world_objects(id) ON DELETE CASCADE,
        FOREIGN KEY (production_id) REFERENCES productions(id) ON DELETE CASCADE,
        FOREIGN KEY (parent_state_id) REFERENCES persistent_world_object_states(id) ON DELETE SET NULL
      )`,
      `CREATE INDEX IF NOT EXISTS idx_persistent_world_object_states_object ON persistent_world_object_states(object_id, persistence, created_at)`,
      `CREATE INDEX IF NOT EXISTS idx_persistent_world_object_states_prod ON persistent_world_object_states(production_id, status, created_at)`,
