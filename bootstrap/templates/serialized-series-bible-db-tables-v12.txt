      // Phase 11.12.1 serialized narrative Series Bible
      `CREATE TABLE IF NOT EXISTS serialized_series_bibles (
        id TEXT PRIMARY KEY,
        namespace TEXT NOT NULL DEFAULT 'default',
        series_key TEXT NOT NULL,
        title TEXT NOT NULL,
        format TEXT NOT NULL DEFAULT 'serialized',
        genres TEXT NOT NULL DEFAULT '[]',
        premise TEXT NOT NULL DEFAULT '',
        world_rules TEXT NOT NULL DEFAULT '[]',
        immutable_canon TEXT NOT NULL DEFAULT '[]',
        narrative_rules TEXT NOT NULL DEFAULT '[]',
        central_conflicts TEXT NOT NULL DEFAULT '[]',
        planned_ending TEXT NOT NULL DEFAULT 'null',
        current_episode INTEGER NOT NULL DEFAULT 0,
        canon_version INTEGER NOT NULL DEFAULT 1,
        revision_number INTEGER NOT NULL DEFAULT 1,
        status TEXT NOT NULL DEFAULT 'active',
        created_by TEXT,
        created_at TEXT NOT NULL,
        updated_at TEXT NOT NULL,
        UNIQUE(namespace, series_key)
      )`,
      `CREATE INDEX IF NOT EXISTS idx_serialized_series_bibles_namespace ON serialized_series_bibles(namespace, status, updated_at)`,
      `CREATE TABLE IF NOT EXISTS serialized_series_bible_revisions (
        id TEXT PRIMARY KEY,
        series_id TEXT NOT NULL,
        revision_number INTEGER NOT NULL,
        canon_version INTEGER NOT NULL,
        snapshot TEXT NOT NULL DEFAULT '{}',
        change_kind TEXT NOT NULL DEFAULT 'update',
        change_reason TEXT NOT NULL DEFAULT '',
        actor TEXT,
        created_at TEXT NOT NULL,
        UNIQUE(series_id, revision_number),
        FOREIGN KEY (series_id) REFERENCES serialized_series_bibles(id) ON DELETE CASCADE
      )`,
      `CREATE INDEX IF NOT EXISTS idx_serialized_series_bible_revisions_series ON serialized_series_bible_revisions(series_id, revision_number DESC)`,
      `CREATE TABLE IF NOT EXISTS serialized_series_strategy_bindings (
        strategy_id TEXT PRIMARY KEY,
        series_id TEXT NOT NULL,
        episode_number INTEGER NOT NULL DEFAULT 1,
        actor TEXT,
        reason TEXT NOT NULL DEFAULT '',
        created_at TEXT NOT NULL,
        updated_at TEXT NOT NULL,
        FOREIGN KEY (series_id) REFERENCES serialized_series_bibles(id) ON DELETE CASCADE
      )`,
      `CREATE INDEX IF NOT EXISTS idx_serialized_series_strategy_bindings_series ON serialized_series_strategy_bindings(series_id, episode_number)`,
