      // Phase 11.12.4 serialized Character Arc Memory
      `CREATE TABLE IF NOT EXISTS serialized_character_arcs (
        id TEXT PRIMARY KEY,
        series_id TEXT NOT NULL,
        character_key TEXT NOT NULL,
        display_name TEXT NOT NULL,
        persistent_character_id TEXT,
        persistent_character_fingerprint TEXT,
        arc_phase TEXT NOT NULL DEFAULT '',
        story_status TEXT NOT NULL DEFAULT 'active',
        emotional_state TEXT NOT NULL DEFAULT '',
        moral_state TEXT NOT NULL DEFAULT '',
        physical_condition TEXT NOT NULL DEFAULT '',
        goals TEXT NOT NULL DEFAULT '[]',
        motivations TEXT NOT NULL DEFAULT '[]',
        beliefs TEXT NOT NULL DEFAULT '[]',
        knowledge TEXT NOT NULL DEFAULT '[]',
        secrets TEXT NOT NULL DEFAULT '[]',
        inner_conflicts TEXT NOT NULL DEFAULT '[]',
        commitments TEXT NOT NULL DEFAULT '[]',
        milestones TEXT NOT NULL DEFAULT '[]',
        notes TEXT NOT NULL DEFAULT '',
        first_episode_number INTEGER NOT NULL DEFAULT 0,
        last_episode_number INTEGER NOT NULL DEFAULT 0,
        revision_number INTEGER NOT NULL DEFAULT 0,
        status TEXT NOT NULL DEFAULT 'active',
        created_at TEXT NOT NULL,
        updated_at TEXT NOT NULL,
        UNIQUE(series_id, character_key),
        FOREIGN KEY (series_id) REFERENCES serialized_series_bibles(id) ON DELETE CASCADE,
        FOREIGN KEY (persistent_character_id) REFERENCES persistent_characters(id) ON DELETE SET NULL
      )`,
      `CREATE INDEX IF NOT EXISTS idx_serialized_character_arcs_series ON serialized_character_arcs(series_id, status, last_episode_number DESC)`,
      `CREATE INDEX IF NOT EXISTS idx_serialized_character_arcs_visual ON serialized_character_arcs(persistent_character_id)`,
      `CREATE TABLE IF NOT EXISTS serialized_character_arc_revisions (
        id TEXT PRIMARY KEY,
        character_arc_id TEXT NOT NULL,
        series_id TEXT NOT NULL,
        episode_number INTEGER NOT NULL,
        revision_number INTEGER NOT NULL,
        snapshot TEXT NOT NULL DEFAULT '{}',
        change_kind TEXT NOT NULL DEFAULT 'episode_commit',
        change_reason TEXT NOT NULL DEFAULT '',
        actor TEXT,
        created_at TEXT NOT NULL,
        UNIQUE(character_arc_id, revision_number),
        FOREIGN KEY (character_arc_id) REFERENCES serialized_character_arcs(id) ON DELETE CASCADE,
        FOREIGN KEY (series_id) REFERENCES serialized_series_bibles(id) ON DELETE CASCADE
      )`,
      `CREATE INDEX IF NOT EXISTS idx_serialized_character_arc_revisions_arc ON serialized_character_arc_revisions(character_arc_id, revision_number DESC)`,
      `CREATE TABLE IF NOT EXISTS serialized_character_arc_episode_commits (
        id TEXT PRIMARY KEY,
        series_id TEXT NOT NULL,
        character_arc_id TEXT NOT NULL,
        episode_memory_id TEXT NOT NULL,
        episode_number INTEGER NOT NULL,
        arc_revision_number INTEGER NOT NULL,
        commit_kind TEXT NOT NULL DEFAULT 'episode_commit',
        timeline_event_ids TEXT NOT NULL DEFAULT '[]',
        before_snapshot TEXT NOT NULL DEFAULT '{}',
        after_snapshot TEXT NOT NULL DEFAULT '{}',
        change_summary TEXT NOT NULL DEFAULT '',
        reason TEXT NOT NULL DEFAULT '',
        actor TEXT,
        created_at TEXT NOT NULL,
        FOREIGN KEY (series_id) REFERENCES serialized_series_bibles(id) ON DELETE CASCADE,
        FOREIGN KEY (character_arc_id) REFERENCES serialized_character_arcs(id) ON DELETE CASCADE,
        FOREIGN KEY (episode_memory_id) REFERENCES serialized_episode_memories(id) ON DELETE CASCADE
      )`,
      `CREATE INDEX IF NOT EXISTS idx_serialized_character_arc_commits_arc ON serialized_character_arc_episode_commits(character_arc_id, episode_number DESC, arc_revision_number DESC)`,
      `CREATE INDEX IF NOT EXISTS idx_serialized_character_arc_commits_series ON serialized_character_arc_episode_commits(series_id, episode_number DESC, created_at DESC)`,
